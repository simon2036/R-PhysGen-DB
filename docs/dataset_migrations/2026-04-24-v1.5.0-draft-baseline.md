---
migration_id: 2026-04-24-v1.5.0-draft-baseline
target_version: v1.5.0-draft
compatibility: additive
rebuild_required: true
affected_layers:
  - raw
  - bronze
  - silver
  - gold
  - extensions
  - schemas
  - docs
  - pipeline
  - duckdb
  - tests
  - ci
migration_script: none
review_status: applied
---

# Dataset Migration: v1.5.0-draft baseline

## Summary

This record establishes `v1.5.0-draft` as the baseline dataset migration registry entry after the PR-A through PR-H P0 review-response work.

The baseline includes staged orchestration, condition sets, structured cycle operating points, deterministic screening proxy features, optional quantum pilot ingestion, governed mixture tables, active-learning queue outputs, canonical property governance outputs, `data/gold/VERSION`, and the rebuilt `data/index/r_physgen_v2.duckdb` query index.

## Compatibility

This is treated as an additive baseline for future migration tracking. Historical PR-A through PR-H changes are not split into separate migration records.

Consumers should continue using the existing Parquet outputs and the DuckDB index generated by the build pipeline. ML-facing consumers should prefer `data/gold/property_recommended_canonical_strict.parquet` for strict canonical features.

## Required Actions

- Keep `data/gold/VERSION` at `v1.5.0-draft` until a later migration changes the dataset version.
- Rebuild Parquet and DuckDB outputs through the existing pipeline when generated artifacts need refresh.
- Do not run an external migration script; `migration_script` is `none`.

## Verification

Baseline verification commands recorded for this version:

```powershell
.venv\Scripts\python.exe pipelines\build_v1_dataset.py
.venv\Scripts\python.exe pipelines\validate_v1_dataset.py
.venv\Scripts\pytest.exe -q
```
