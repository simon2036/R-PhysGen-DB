---
migration_id: 2026-04-30-v1.6.3-draft-residual-computation-pass
target_version: v1.6.3-draft
compatibility: additive
rebuild_required: true
affected_layers:
  - raw
  - bronze
  - silver
  - gold
  - docs
  - pipeline
  - tests
migration_script: none
review_status: applied
---

# Dataset Migration: v1.6.3-draft residual computation pass

## Summary

This migration follows the phase-2 quantum handoff with the remaining computation tasks that can be completed on the current local machine. It does not change the dataset version; it makes the `v1.6.3-draft` state more explicit by reconciling completed Psi4 DFT requests, retrying the two xTB Hessian imaginary-frequency cases, and ingesting the open-source cycle backend rows that CoolProp can resolve.

## Execution state

- Psi4 DFT expansion manifest: `150` request IDs are now marked `completed` because all `150` already have six completed Psi4 scalar rows in `data/lake/raw/manual/quantum_pilot_results.csv` (`900` rows total). The runner resumed with `requested=0`, `resumed=205`.
- xTB Hessian imaginary-frequency retries: `2` request IDs were rerun with the local xTB executor. Both reproduced `imaginary_frequency_count=1`, so accepted Hessian coverage remains `60` accepted jobs and `240` scalar rows.
- Cycle backend retry: `21` active-learning `run_cycle` entries were attempted with backend `auto`. CoolProp succeeded for `4` queue entries and wrote `16` cycle rows; `17` queue entries remain blocked because REFPROP is not configured and CoolProp could not satisfy the requested backend targets.
- ORCA opt/freq: still blocked. `_resolve_orca_bin(None)` and `_resolve_orca_bin('/usr/bin/orca')` both resolve to `None`; `/usr/bin/orca` is not accepted as the ORCA quantum-chemistry package.

## Added / refreshed artifacts

- `data/lake/raw/manual/cycle_backend_results.csv`
- `data/lake/raw/manual/quantum_pilot_results.csv`
- `data/lake/raw/manual/quantum_phase2_vibrational_modes.csv`
- `data/lake/raw/generated/quantum_dft_requests.csv`
- `data/lake/raw/generated/quantum_dft_xyz_manifest.csv`
- `data/lake/raw/generated/active_learning_queue.csv`
- rebuilt bronze/silver/gold Parquet tables and DuckDB index

The following local executor scratch trees remain intentionally uncommitted:

- `data/lake/raw/manual/quantum_phase2_artifacts/`
- `data/lake/raw/manual/quantum_pilot_artifacts/`
- `data/lake/raw/manual/cycle_backend_artifacts/`

## Post-pass counts

- `property_observation`: `60,752`
- `property_recommended`: `56,770`
- `property_recommended_canonical`: `57,174`
- `observation_condition_set`: `110`
- `cycle_case`: `7`
- `active_learning_queue`: `2,088` rows (`2,000` completed `run_quantum`, `88` proposed follow-ups)
- remaining proposed `run_cycle`: `17`
- research-task readiness: `5 passed`
- validation errors: `0`

## Remaining blockers

- ORCA opt/freq needs a real ORCA quantum-chemistry executable and a separate execution/ingestion pass.
- The two xTB Hessian structures with `imaginary_frequency_count=1` need a separate conformer/geometry remediation contract before they can become accepted Hessian observations.
- The `17` unresolved cycle entries require REFPROP configuration or a curated backend/alias strategy beyond current CoolProp coverage.
- The `73` unmapped governance substances and `197` phase-2 blockers still need curation/mapping rather than executor time.

## Verification

```bash
PYTHONPATH=src .venv/bin/python pipelines/run_xtb_phase2_hessian.py --requests <2-row retry manifest> --xyz-manifest <2-row retry xyz> --xtb-bin /home/biosphere/micromamba/envs/rh_round1/bin/xtb --jobs 2 --threads-per-job 1 --no-resume
PYTHONPATH=src .venv/bin/python pipelines/run_remaining_cycle_computations.py --backend auto --write-results
PYTHONPATH=src .venv/bin/python pipelines/run_psi4_quantum_pilot.py --requests data/lake/raw/generated/quantum_dft_requests.csv --xyz-manifest data/lake/raw/generated/quantum_dft_xyz_manifest.csv --psi4-bin /home/biosphere/micromamba/envs/rphysgen-psi4/bin/psi4 --scratch-dir data/lake/raw/manual/quantum_pilot_artifacts/_psi4_scratch --jobs 8
PYTHONPATH=src .venv/bin/python pipelines/build_v1_dataset.py
PYTHONPATH=src .venv/bin/python pipelines/validate_v1_dataset.py
PYTHONPATH=src .venv/bin/python -m pytest -q
```
