table_name: property_dictionary
layer: gold
path: data/lake/gold/property_dictionary.parquet
primary_key:
  - canonical_property_id
columns:
  - {name: canonical_property_id, dtype: string, required: true}
  - {name: canonical_property_group, dtype: string, required: true}
  - {name: canonical_property_name, dtype: string, required: true}
  - {name: canonical_feature_key, dtype: string, required: true}
  - {name: standard_unit, dtype: string, required: true}
